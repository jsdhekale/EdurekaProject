- hosts: webservers
  become: true
  become_user: root
  tasks:
  - name: install tomcat
    yum: name=tomcat state=present
  - name: start tomcat
    service: name=tomcat state=started
  - name: Install git & docker
    yum: name={{item}} state=present
    loop:
    - git
    - docker
  - name: start docker
    service: name=docker state=started
  - name: clone my repo
    git: repo=https://github.com/jsdhekale/AnsibleDockerdemo.git dest=/tmp/gitrepo
  - name: build the dockerfile
    command: chdir=/tmp/gitrepo build -t myimage:edurekaproject21 .
  - name: Run the image to deploy the app
    command: docker run -itd -p myimage:edurekaproject21
